<p id="notice"><%= notice %></p>

<p>
  <strong>Paypal payer:</strong>
  <%= subscription.paypal_payer_id %>
</p>

<p>
  <strong>Paypal profile:</strong>
  <%= subscription.paypal_profile_id %>
</p>

<p>
  <strong>Paid until:</strong>
  <%= subscription.paid_until %>
</p>

<p>
  <strong>Plan:</strong>
  <%= subscription.plan %>
</p>

<% if subscription.paid? %>
  <%= link_to 'Cancel',
               subscription_path(subscription),
               method: :delete,
               data: { confirm: 'Are you sure that you want cancel your subscription?' } %> |
<% end %>
<%= link_to 'Back', subscriptions_path %>
